name: Web CI (Smoke)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Smoke check
        run: |
          echo "Zoryu Trade Web CI: OK"
          echo "Repo is reachable and workflow is configured."
